Utilisation de ruhoh :

Pour le lancement en local : 
```shell
bundle exec ruhoh server 9292
```

Pour la génération des pages (pushées sous un dépôt github pages) :
```shell
bundle exec ruhoh compile
```

le site généré sera présent dans le répertoire `compiled`
